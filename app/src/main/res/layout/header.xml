<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/topBar" android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="10dp"
    android:background="?attr/colorHeaderBackground"
    android:gravity="center_vertical">

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/farmer_icon" />

    <TextView
        android:id="@+id/userEmail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="mada.petru@digitalexplorer.ro"
        android:textColor="?attr/colorHeaderText"
        android:textSize="14sp" />

    <Button
        android:id="@+id/weatherButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:text="☀️"
        android:gravity="center"
        android:textSize="18sp"
        android:background="?attr/selectableItemBackgroundBorderless" />

    <Button
        android:id="@+id/lockButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:text="🔓"
        android:gravity="center"
        android:textSize="18sp"
        android:background="?attr/selectableItemBackgroundBorderless" />
</LinearLayout>