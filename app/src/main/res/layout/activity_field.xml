<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#87CEEB">

    <!-- Top Bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#66CDAA"
        android:padding="10dp"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/farmer_icon" />

        <TextView
            android:id="@+id/userEmail"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="mada.petru@digitalexplorer.ro"
            android:textColor="@android:color/black"
            android:textSize="14sp" />

        <TextView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:text="☀️"
            android:gravity="center"
            android:textSize="18sp" />

        <TextView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:text="🔓"
            android:gravity="center"
            android:textSize="18sp" />
    </LinearLayout>

    <!-- Title -->
    <TextView
        android:id="@+id/tvFieldName"
        android:layout_below="@id/topBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Field: B"
        android:textStyle="bold"
        android:padding="10dp"
        android:textSize="18sp" />

    <!-- Controls -->
    <LinearLayout
        android:id="@+id/operationControls"
        android:layout_below="@id/tvFieldName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="10dp"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/btnCreateOperation"
            android:layout_width="wrap_content"
            android:background="#66CDAA"
            android:layout_height="wrap_content"
            android:text="Create Operation" />

        <EditText
            android:id="@+id/searchOperation"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="#66CDAA"
            android:layout_marginStart="8dp"
            android:hint="Search operation..." />
    </LinearLayout>

    <!-- RecyclerView Table -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvOperations"
        android:layout_below="@id/operationControls"
        android:layout_above="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="10dp" />

    <!-- Bottom Image -->

    <LinearLayout
        android:id="@+id/bottomNav"
        android:layout_alignParentBottom="true"
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/land">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/farm_tractor"
            android:layout_marginEnd="80dp" />

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="120dp"
            android:src="@drawable/farm" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#66CDAA"
            app:menu="@menu/bottom_nav_menu" />
    </LinearLayout>
</RelativeLayout>
